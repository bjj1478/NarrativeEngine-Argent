# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
/data/
build-output
.exe
.exe-new
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/

# ── Campaign data: never commit. This is the player's own material. ──
# The whole `data/` tree (above) already covers campaigns, backups, embeddings.db,
# settings.json and apikeys.vault. These are the paths that live outside it.
#
# Portraits: generated NPC art AND every image pasted into chat (Vision v1.5),
# so this is personal content, not build output. The old rule only caught *.png
# and leaked .jpg/.webp attachments.
# NOTE: deliberately scoped to portraits/ — `public/assets/Snow Asset Pack/` is a
# licensed art pack and stays tracked.
#
# THIS FORK: portraits are written to `data/portraits/` instead — see
# PUBLIC_ASSETS_DIR in server/lib/fileStore.js. Writing them under public/ put
# them inside Vite's publicDir, so every generated portrait full-reloaded the
# running session. `data/` is already ignored above and is excluded from the
# dev-server watcher. This rule is kept only so any leftover files from before
# that change stay out of commits.
public/assets/portraits/

# Debug / temp scripts
debug_api.js
debug_campaigns.cjs
compare.py

# Build artifacts
server.bundle.cjs

# Debug dumps
scene310_*.txt
scene310_*.md

# Repomix / codebase export files
repomix-output.xml
repomix-output.md
*.docx

# Upgrade / AI planning docs
Upgrade/
PLAN-*.md
GLM-CODE-REVIEW-HANDOFF.md
# SESSION_LOG.md is a local working journal — keep it out of git going forward
Upgrade/Refactor 18-07/SESSION_LOG.md

# Playwright run output (the e2e specs themselves are tracked)
test-results/
playwright-report/

# Graphify output and tooling
graphify-out/
.graphify_python
.graphifyignore
scripts/patch-graph-imports.mjs

# Vite/React scaffold defaults
public/vite.svg
src/assets/react.svg

# Planning / design docs (not app code)
auto-condenser.md

# Electron (not maintained in repo)
electron/
build-server.mjs

# One-time migration scripts
server/scripts/migrateEmbeddings.js

# Debug / stabilization config
stabs.json

# Local non-SRD compendium research; never distribute with the project
Example_Setup/Ability Compendium/Dungeons and Dragons 5e 2024/dnd_2024_full_reference_ability_compendium.json

# Local trash folder
TRASH/

# AI context / codebase map docs (regenerated locally by AI agents)
AI_CODEBASE_MAP.md
ARCHITECTURE.md
CLAUDE.md
AGENTS.md
FEATURE_INVENTORY.md
INNOVATIONS.md
Upgrade/HEIST PLANNING/02_MEMORY_LOD_BRIEFING.md
Upgrade/HEIST PLANNING/03_HANDOFF_PROMPT.md

